import { Button } from '@mui/material';
import AppBar from '@/components/AppBar';

function App() {
  return (
    <div className="App">
      <AppBar children={<></>} />
    </div>
  );
}

export default App;
